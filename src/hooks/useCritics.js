// Read-only hook — data is fetched in App's main data load and passed as prop
// Exported for import convenience; components receive critics as a prop from App.jsx
export function useCritics() {
  return {}
}
